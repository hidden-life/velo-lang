# Velo MVP 0.10 status
MVP 0.10 focuses on annotation-based HTTP routing foundation.

MVP 0.9 introduced a working HTTP server mode with conventional `handle(req)`
and explicit routing helpers.

MVP 0.10 adds function annotations, validates HTTP route annotations, stores annotation metadata
in IR, and connects route annotations to `velo serve`.

## Roadmap
```text
0.10.1      lexer/parser/AST support for function annotations
0.10.2      semantic validation for annotations
0.10.3      IR/module metadata propagation
0.10.4      HTTP route annotation validation
0.10.5      serve mode annotation route dispatch
0.10.6      docs/examples/release checklist
```

## Final status
```text
MVP 0.10    complete
```

## Completed scope
- `@` lexer token
- function annotation AST metadata
- parser support for annotations before functions
- AST printer support
- generic semantic validation for annotations
- qualified annotation import validation
- duplicate annotation validation
- IR annotation metadata
- import alias normalization during lowering
- HTTP route annotation validation
- HTTP route table from IR metadata
- `velo serve` dispatch by route annotations
- conventional `handle(req)` backward compatibility
- examples
- release notes
- release checklist

## Main example
```velo
module app;

use std::http;

@http::get("/health")
fn health(req: http_request): http_response {
    return http::text_response(200, "OK");
}

@http::post("/echo")
fn echo(req: http_request): http_response {
    return http::text_response(201, http::request_body(req));
}

fn main(): int {
    return 0;
}
```

Run:
```bash
./build/debug/apps/velo/velo serve examples/http_annotation_routes/main.velo
```

Smoke:
```bash
curl -i http://127.0.0.1:8080/health
curl -i -X POST http://127.0.0.1:8080/echo -d 'hello'
curl -i http://127.0.0.1:8080/missing
```

## Compatibility
Programs without HTTP route annotations still use the conventional handler:
```velo
fn handle(req: http_request): http_response {
    return http::text_response(200, "OK");
}
```

## Deferred work
- path params
- query parser
- request headers helpers
- middleware
- route groups
- route priority
- more HTTP methods
- async runtime
- keep-alive
- chunked transfer encoding
- TLS
- bytecode annotation metadata
- custom annotation declarations
