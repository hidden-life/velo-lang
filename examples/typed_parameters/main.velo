module app;

use std::console;

fn isReady(value: bool): bool {
    return value;
}

fn echo(value: string): string {
    return value;
}

fn main(): int {
    if (isReady(true)) {
        console::println(echo("typed parameters work"));
        return 0;
    }

    return 0;
}