module app;

use std::console;

fn main(): int {
    let x: int = 5;

    if (x > 0 && x < 10) {
        console::println("logical condition is true");
        return 0;
    }

    console::println("logical condition is false");
    return 0;
}